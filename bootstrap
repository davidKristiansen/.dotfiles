#!/bin/bash

stow \
  --target=$HOME \
  --no-folding \
  --ignore=bootstrap \
  --adopt \
  .

git restore .


# Install asdf plugins
cut -d' ' -f1 .tool-versions|xargs -i asdf plugin add  {}
asdf install
