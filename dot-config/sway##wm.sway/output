# Wallpaper / background. Solid color; swap for `bg <path> fill` for an image.
output * bg #1d2021 solid_color

# Per-output geometry is managed by kanshi (see startup_applications).

# vim: filetype=i3config
