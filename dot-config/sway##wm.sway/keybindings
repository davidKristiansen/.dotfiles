# Keybindings, grouped: basics, focus/move, workspaces, layout, scratchpad,
# media/hardware, notifications, modes, gestures.

# Hold $mod + left mouse button to drag windows, right mouse button to
# resize, middle mouse button (anywhere in the window) to kill. Despite
# the name, this also works for non-floating windows.
floating_modifier $mod normal

#
# Basics
#
bindsym $mod+Return exec $term
bindsym $mod+d exec $menu
bindsym $mod+Shift+d exec $menu_fallback
bindsym $mod+Shift+q kill
bindsym button2 kill
bindsym --whole-window $mod+button2 kill
bindsym $mod+Shift+s mode $mode_screenshot
bindsym $mod+Delete exec $lock
bindsym $mod+z exec systemctl suspend
bindsym $mod+t exec $HOME/.config/waybar/teams-menu.sh toggle
# Quick command runner — a one-line "Run" box (systemctl, etc.); type a
# command, Enter runs it and the box closes. Like Windows' Run dialog.
# Re-pressing $mod+r replaces any open box. Keeps your zsh completions.
bindsym $mod+r exec $XDG_BIN_HOME/quickcmd.sh
bindsym $mod+Shift+c reload
bindsym $mod+Shift+e exec swaynag -t warning \
    -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' \
    -b 'Yes, exit sway' 'systemctl --user stop sway-session.target; swaymsg exit'

#
# Focus / move — $mod+hjkl or arrows; add Shift to move the window
#
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

bindsym $mod+a focus parent

#
# Workspaces — switch with $mod+N, move window with $mod+Shift+N
#
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

bindsym $mod+Control+left workspace prev
bindsym $mod+Control+right workspace next
bindsym $mod+Tab workspace back_and_forth

# Move a whole workspace to another output
bindsym Alt+Control+left move workspace to output left
bindsym Alt+Control+right move workspace to output right

#
# Layout
#
bindsym $mod+b splith
bindsym $mod+v splitv
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split
bindsym $mod+f fullscreen
bindsym $mod+Shift+space floating toggle

#
# Scratchpad — app-specific bindings live in the scratchpad file
#
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show

#
# Media & hardware — --locked so they work on the lockscreen too
#
bindsym --locked XF86AudioRaiseVolume exec wpctl set-volume -l 1.0 @DEFAULT_AUDIO_SINK@ 5%+
bindsym --locked XF86AudioLowerVolume exec wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindsym --locked XF86AudioMute exec wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindsym --locked XF86AudioMicMute exec wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindsym --locked XF86MonBrightnessDown exec $XDG_BIN_HOME/backlight.sh down 5
bindsym --locked XF86MonBrightnessUp exec $XDG_BIN_HOME/backlight.sh up 5
bindsym --locked $mod+Space exec playerctl play-pause
bindsym --locked $mod+n exec playerctl next
bindsym --locked $mod+p exec playerctl previous

#
# Notifications (mako) — $mod+n/p are taken by playerctl, so dismiss lives
# on $mod+Control+n
#
bindsym $mod+Shift+n exec makoctl mode -t do-not-disturb && pkill -RTMIN+9 waybar
bindsym $mod+Control+n exec makoctl dismiss
bindsym $mod+Control+Shift+n exec makoctl dismiss --all

#
# Input — both bindings route through input.sh (shared with waybar's
# custom/input module, which it signals for an instant repaint):
#   Ctrl+Space        cycle the input state: en → no → ja → en
#   Ctrl+Shift+Space  toggle fcitx (Japanese/Mozc) directly
# fcitx's own default trigger (Ctrl+Space) is harmlessly shadowed — sway grabs
# the key first — so the cycle stays entirely with input.sh.
#
bindsym Control+space exec $XDG_BIN_HOME/input.sh cycle
bindsym Control+Shift+space exec $XDG_BIN_HOME/input.sh ja

#
# Modes (defined in functions)
#
bindsym $mod+Shift+r mode "resize"
bindsym $mod+g mode "gaps"

#
# Touchpad gestures (native libinput — no external daemon needed)
#
# Three fingers: switch workspaces (natural scrolling — swipe right goes to
# the previous workspace, swipe left to the next).
bindgesture swipe:3:left  workspace prev
bindgesture swipe:3:right workspace next

# Four fingers: move the focused container between workspaces.
bindgesture swipe:4:left  move container to workspace prev, workspace prev
bindgesture swipe:4:right move container to workspace next, workspace next

# Pinch in/out toggles floating on the focused window.
bindgesture pinch:inward  floating toggle
bindgesture pinch:outward floating toggle

# vim: filetype=i3config
