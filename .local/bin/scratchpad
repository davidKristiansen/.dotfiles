#!/bin/bash

if [[ ! $(ps aux | grep "[_]_scratchpad") ]]; then
  kitty --class __scratchpad; swaymsg '[app_id="__scratchpad"] scratchpad show'
fi

swaymsg '[app_id="__scratchpad"] scratchpad show'

# vim.filetype=bash:
