[Addon]
# Disable the StatusNotifier (SNI) tray icon. The current input method is
# surfaced by the waybar custom/fcitx module (あ) instead, so the tray icon is
# redundant. Addon enable/disable overrides are read from the *data* dir
# (~/.local/share/fcitx5/addon/), not ~/.config/fcitx5 — the config-dir path
# has no effect. Right-click config is still reachable via `fcitx5-config-qt`.
Enabled=False
