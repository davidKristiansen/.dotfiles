# Project management — not dotfiles
/AGENTS.md
bootstrap
.pre-commit-config.yaml
.secrets.baseline

# GitHub repo config
.github
.gitignore
.gitmodules

# Claude Code project-local state for this repo (not the dot-claude package).
# Anchored: an unanchored '.claude' also matches dot-config/nvim/.claude, which
# is tracked content, and stow.sh refuses to evict tracked files.
/.claude
